<!doctype html><html><head><meta charset="utf-8">@include('reports._style')</head><body>
<div class="brand"><h1>{{ $proyecto->codigo }} · {{ $proyecto->nombre }}</h1><div class="muted">Empresa: {{ $proyecto->empresa->nombre_comercial }} · Cliente: {{ $proyecto->cliente->nombre }}</div></div>
<table class="grid"><tr><th>Fase</th><td>{{ str_replace('_',' ',$proyecto->fase) }}</td><th>Progreso</th><td>{{ $proyecto->progreso }}%</td></tr><tr><th>Estado</th><td>{{ $proyecto->estado }}</td><th>Responsable</th><td>{{ $proyecto->responsable?->nombre }} {{ $proyecto->responsable?->apellido }}</td></tr><tr><th>Inicio</th><td>{{ $proyecto->fecha_inicio?->format('d/m/Y') }}</td><th>Entrega</th><td>{{ $proyecto->fecha_entrega?->format('d/m/Y') }}</td></tr></table>
<h2>Descripción</h2><div class="answer">{{ $proyecto->descripcion ?: 'Sin descripción' }}</div>
<h2>Historial de avances</h2><table class="grid"><thead><tr><th>Fecha</th><th>Fase / área</th><th>Avance</th><th>Descripción</th><th>Progreso</th><th>Anexos</th></tr></thead><tbody>@forelse($proyecto->avances as $avance)<tr><td>{{ $avance->created_at->format('d/m/Y H:i') }}</td><td>{{ $avance->fase }} / {{ $avance->area ?? 'general' }}</td><td>{{ $avance->titulo }}</td><td>{{ $avance->descripcion }}</td><td>{{ $avance->progreso }}%</td><td>@forelse($avance->archivos as $archivo){{ $archivo->nombre_original }}@if(!$loop->last)<br>@endif @empty Sin anexos @endforelse</td></tr>@empty<tr><td colspan="6">Sin avances registrados.</td></tr>@endforelse</tbody></table>
<div class="footer">VITI · Resumen del proyecto</div></body></html>
